{
  "name": "crv",
  "version": "0.1.0-beta.0",
  "description": "Canton validator recovery-precondition verifier",
  "license": "MIT",
  "type": "module",
  "bin": {
    "crv": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "docs/report-schema-v1.json",
    "docs/manifest-schema-v1.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "npm test"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
