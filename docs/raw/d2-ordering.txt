The injected contract ID and all Canton identities are redacted. Counts are
preserved.

$ query offsets before participant capture (t1)
participant_max_event_offset=59
validator_last_ingested_offset=00000000000000003b

$ pg_dumpall (t1)
bytes=146906506
elapsed=5.24

$ pg_dump participant-app-provider (t1)
bytes=45617771
elapsed=0.76

$ POST /api/validator/v0/wallet/tap
contract_id=<local-fixture-contract-id>

$ query offsets after the operation (t2)
participant_max_event_offset=62
validator_last_ingested_offset=00000000000000003e

$ pg_dump validator-app-provider (t2)
bytes=176643
elapsed=0.38

$ count injected ID in dumps
validator_t2=4
participant_t1=0

$ restore base t1; overwrite validator app with t2
base_restore_elapsed=18.09
validator_overwrite_elapsed=1.04
participant_max_event_offset=59
validator_last_ingested_offset=00000000000000003e

$ start restored Canton + Splice stack on internal network
network_internal=true
canton_health=healthy
splice_health=healthy
validator_reference_count=4
participant_reference_count=0
offset_or_failed_precondition_error_count=0

The pair is semantically reversed by construction. PostgreSQL restored it and
the full application stack stayed healthy without a generic offset error.
